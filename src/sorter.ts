'use strict';

// import { window, Range, Position, TextEditor } from 'vscode';

export enum SortType {
  ALPHABETIC = 'alphabetic',
  SEPARATE_DENDENCIES = 'separate-dependencies',
}

export function sort(type:SortType = SortType.ALPHABETIC) {

}